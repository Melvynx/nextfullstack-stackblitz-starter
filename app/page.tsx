import { Card, CardHeader, CardTitle } from "@/components/ui/card";

export default async function RoutePage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Stackblitz start</CardTitle>
      </CardHeader>
    </Card>
  );
}
